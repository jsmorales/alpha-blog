
<%= will_paginate %>

<% @categories.each do |category| %>
	<h3><%= link_to category.name, category_path(category) %></h3>
	<small><%= pluralize(category.articles.count, "artículo") %></small>
<%end%>

